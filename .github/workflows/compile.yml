name: Build ESP8266 Bin

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # v3 မှ v4 သို့ ပြောင်းရန်

      - name: Install Arduino CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
          sudo mv bin/arduino-cli /usr/local/bin/

      - name: Install ESP8266 Core
        run: |
          arduino-cli core update-index --additional-urls https://arduino.esp8266.com/stable/package_esp8266com_index.json
          arduino-cli core install esp8266:esp8266 --additional-urls https://arduino.esp8266.com/stable/package_esp8266com_index.json

      - name: Install Libraries
        run: |
          arduino-cli lib install "ESPAsyncTCP"
          arduino-cli lib install "ESP Async WebServer"
          arduino-cli lib install "ArduinoJson"

      - name: Compile Sketch
        run: |
          # main.ino ဖိုင်နာမည် မှန်အောင် စစ်ပါ (ဥပမာ- sketch_name.ino ဆိုရင် အဲဒီအတိုင်း ပြောင်းရေးပါ)
          arduino-cli compile --fqbn esp8266:esp8266:nodemcuv2 --output-dir ./build ./main.ino

      - name: Upload Binary
        uses: actions/upload-artifact@v4  # v3 မှ v4 သို့ ပြောင်းရန်
        with:
          name: esp8266-firmware
          path: ./build/*.bin
          
